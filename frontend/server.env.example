# Production builds use frontend/.env.production (committed) for the Render backend URL.
# Override in Vercel Environment Variables if needed.
NEXT_PUBLIC_API_BASE_URL=https://agrovision-ai-app.onrender.com
