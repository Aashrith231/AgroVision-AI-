# Set this in Vercel after the college backend URL is ready.
# Prefer HTTPS if the college server provides a domain or reverse proxy.
NEXT_PUBLIC_API_BASE_URL=https://your-college-server-domain-or-ip
